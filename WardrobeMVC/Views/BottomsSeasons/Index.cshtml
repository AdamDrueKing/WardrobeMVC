@model IEnumerable<WardrobeMVC.Models.BottomsSeason>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BottomsSeasonName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BottomsSeasonName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BottomsSeasonID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BottomsSeasonID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BottomsSeasonID })
        </td>
    </tr>
}

</table>
