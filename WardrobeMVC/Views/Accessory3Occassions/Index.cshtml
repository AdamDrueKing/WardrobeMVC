@model IEnumerable<WardrobeMVC.Models.Accessory3Occassions>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Accessory3OccassionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Accessory3OccassionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Accessory3OccassionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Accessory3OccassionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Accessory3OccassionID })
        </td>
    </tr>
}

</table>
