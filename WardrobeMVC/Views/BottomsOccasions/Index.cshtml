@model IEnumerable<WardrobeMVC.Models.BottomsOccasion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BottomsOccasionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BottomsOccasionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BottomsOccasionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BottomsOccasionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BottomsOccasionID })
        </td>
    </tr>
}

</table>
