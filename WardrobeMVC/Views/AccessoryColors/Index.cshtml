@model IEnumerable<WardrobeMVC.Models.AccessoryColor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AccessoryColorName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AccessoryColorName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AccessoryColorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AccessoryColorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AccessoryColorID })
        </td>
    </tr>
}

</table>
