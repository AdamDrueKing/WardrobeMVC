@model IEnumerable<WardrobeMVC.Models.BottomsColor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BottomsColorName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BottomsColorName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BottomsColorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BottomsColorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BottomsColorID })
        </td>
    </tr>
}

</table>
