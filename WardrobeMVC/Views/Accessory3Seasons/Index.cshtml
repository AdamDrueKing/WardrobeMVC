@model IEnumerable<WardrobeMVC.Models.Accessory3Seasons>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Accessory3SeasonsName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Accessory3SeasonsName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Accessory3SeasonID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Accessory3SeasonID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Accessory3SeasonID })
        </td>
    </tr>
}

</table>
