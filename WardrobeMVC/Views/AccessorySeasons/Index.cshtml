@model IEnumerable<WardrobeMVC.Models.AccessorySeason>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AccessorySeasonName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AccessorySeasonName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AccessorySeasonID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AccessorySeasonID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AccessorySeasonID })
        </td>
    </tr>
}

</table>
