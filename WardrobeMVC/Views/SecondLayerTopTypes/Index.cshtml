@model IEnumerable<WardrobeMVC.Models.SecondLayerTopType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SecondLayerTopName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SecondLayerTopName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SecondLayerTopTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SecondLayerTopTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SecondLayerTopTypeID })
        </td>
    </tr>
}

</table>
