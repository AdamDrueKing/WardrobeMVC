@model IEnumerable<WardrobeMVC.Models.ShoeColor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ShoeColorName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ShoeColorName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShoeColorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShoeColorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShoeColorID })
        </td>
    </tr>
}

</table>
