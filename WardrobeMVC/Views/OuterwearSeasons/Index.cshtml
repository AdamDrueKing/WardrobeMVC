@model IEnumerable<WardrobeMVC.Models.OuterwearSeason>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OuterwearSeasonName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OuterwearSeasonName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OuterwearSeasonID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OuterwearSeasonID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OuterwearSeasonID })
        </td>
    </tr>
}

</table>
