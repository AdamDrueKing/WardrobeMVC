@model IEnumerable<WardrobeMVC.Models.ShoeOccasion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ShoeOccassionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ShoeOccassionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShoeOccasionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShoeOccasionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShoeOccasionID })
        </td>
    </tr>
}

</table>
