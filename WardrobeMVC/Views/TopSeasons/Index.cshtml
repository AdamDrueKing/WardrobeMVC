@model IEnumerable<WardrobeMVC.Models.TopSeason>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TopSeasonName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TopSeasonName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TopSeasonID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TopSeasonID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TopSeasonID })
        </td>
    </tr>
}

</table>
