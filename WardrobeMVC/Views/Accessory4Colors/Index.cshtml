@model IEnumerable<WardrobeMVC.Models.Accessory4Colors>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Accessory4ColorName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Accessory4ColorName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Accessory4ColorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Accessory4ColorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Accessory4ColorID })
        </td>
    </tr>
}

</table>
