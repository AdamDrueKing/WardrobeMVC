@model IEnumerable<WardrobeMVC.Models.AccessoryOccasion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AccessoryOccasionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AccessoryOccasionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AccessoryOccasionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AccessoryOccasionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AccessoryOccasionID })
        </td>
    </tr>
}

</table>
