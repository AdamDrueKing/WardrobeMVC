@model IEnumerable<WardrobeMVC.Models.Accessory3Types>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Accessory3TypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Accessory3TypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Accessory3TypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Accessory3TypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Accessory3TypeID })
        </td>
    </tr>
}

</table>
