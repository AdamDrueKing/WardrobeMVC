@model IEnumerable<WardrobeMVC.Models.Accessory4Occassions>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Accessory4OccassionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Accessory4OccassionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Accessory4OccassionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Accessory4OccassionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Accessory4OccassionID })
        </td>
    </tr>
}

</table>
