@model IEnumerable<WardrobeMVC.Models.OuterwearType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OuterwearTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OuterwearTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OuterwearTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OuterwearTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OuterwearTypeID })
        </td>
    </tr>
}

</table>
