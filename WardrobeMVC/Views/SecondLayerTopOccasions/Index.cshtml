@model IEnumerable<WardrobeMVC.Models.SecondLayerTopOccasion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SecondLayerTopOccasionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SecondLayerTopOccasionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SecondLayerTopOccasionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SecondLayerTopOccasionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SecondLayerTopOccasionID })
        </td>
    </tr>
}

</table>
