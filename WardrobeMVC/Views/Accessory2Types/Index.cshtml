@model IEnumerable<WardrobeMVC.Models.Accessory2Types>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Accessory2TypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Accessory2TypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Accessory2TypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Accessory2TypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Accessory2TypeID })
        </td>
    </tr>
}

</table>
